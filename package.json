{
  "name": "medicare-pro",
  "version": "3.2.0",
  "private": true,
  "description": "MediCare Pro – Hospital Management System (Firestore + Firebase Auth demo)",
  "scripts": {
    "seed": "node scripts/seed.js",
    "check": "node --check api/login.js && node --check api/firebase-config.js && node --check server.js && node --check js/firebase.js && node --check scripts/seed.js",
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "firebase-admin": "^12.7.0"
  }
}
